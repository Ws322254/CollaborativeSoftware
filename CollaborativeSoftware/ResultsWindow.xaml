<Window x:Class="CollaborativeSoftware.ResultsWindow"
     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Quizelyangs Results History" Height="600" Width="1000"
 WindowStartupLocation="CenterScreen"
 Background="#1a1a2e"
  Loaded="Window_Loaded">
    <Grid>
        <StackPanel Orientation="Vertical" Margin="20">
   <TextBlock Text="Results and History" FontSize="32" FontWeight="Bold" Foreground="#8b3dff" Margin="0,0,0,20"/>
      
       <!-- Statistics -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,30">
         <Border Width="200" Height="100" Background="#2d2d44" CornerRadius="10" Margin="10" Padding="20">
  <StackPanel VerticalAlignment="Center">
       <TextBlock Text="Total Points" FontSize="14" Foreground="#888888"/>
   <TextBlock x:Name="TotalPointsBlock" Text="0" FontSize="28" FontWeight="Bold" Foreground="#8b3dff"/>
    </StackPanel>
            </Border>
 
      <Border Width="200" Height="100" Background="#2d2d44" CornerRadius="10" Margin="10" Padding="20">
          <StackPanel VerticalAlignment="Center">
            <TextBlock Text="Average Score" FontSize="14" Foreground="#888888"/>
   <TextBlock x:Name="AverageScoreBlock" Text="0%" FontSize="28" FontWeight="Bold" Foreground="#00d4ff"/>
        </StackPanel>
 </Border>
       
      <Border Width="200" Height="100" Background="#2d2d44" CornerRadius="10" Margin="10" Padding="20">
<StackPanel VerticalAlignment="Center">
         <TextBlock Text="Tests Taken" FontSize="14" Foreground="#888888"/>
            <TextBlock x:Name="TestsTakenBlock" Text="0" FontSize="28" FontWeight="Bold" Foreground="#28a745"/>
 </StackPanel>
            </Border>
            </StackPanel>

      <!-- Test History Table -->
   <TextBlock Text="Recent Test Results" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>
   <DataGrid x:Name="ResultsDataGrid" Background="#2d2d44" Foreground="White" BorderBrush="#555555" 
    BorderThickness="1" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Height="250" Margin="0,0,0,20">
              <DataGrid.Columns>
     <DataGridTextColumn Header="Quiz" Binding="{Binding Quiz.Title}" Width="250"/>
        <DataGridTextColumn Header="Subject" Binding="{Binding Quiz.Subject.Name}" Width="150"/>
           <DataGridTextColumn Header="Score" Binding="{Binding Score, StringFormat='0%'}" Width="100"/>
        <DataGridTextColumn Header="Points" Binding="{Binding Points}" Width="100"/>
       <DataGridTextColumn Header="Status" Binding="{Binding IsPassed}" Width="100"/>
        <DataGridTextColumn Header="Date" Binding="{Binding CompletedDate, StringFormat='dd MMM yyyy HH:mm'}" Width="150"/>
 </DataGrid.Columns>
 </DataGrid>

  <!-- Navigation Buttons -->
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
  <Button Content="Back to Dashboard" Click="BackButton_Click" Width="150" Height="40" Background="#8b3dff" Foreground="White" FontSize="14" Cursor="Hand" Margin="10"/>
     <Button Content="Leaderboard" Click="LeaderboardButton_Click" Width="150" Height="40" Background="#00d4ff" Foreground="White" FontSize="14" Cursor="Hand" Margin="10"/>
     </StackPanel>
      </StackPanel>
    </Grid>
</Window>
