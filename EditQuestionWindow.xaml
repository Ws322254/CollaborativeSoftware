<Window x:Class="CollaborativeSoftware.EditQuestionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  Title="Edit Question" Height="700" Width="700"
        WindowStartupLocation="CenterOwner"
    Background="#1a1a2e"
   ResizeMode="NoResize">

    <Window.Resources>
   <!-- TextBox Style -->
        <Style x:Key="FormTextBox" TargetType="TextBox">
     <Setter Property="Background" Value="#2d2d44"/>
<Setter Property="Foreground" Value="White"/>
  <Setter Property="BorderBrush" Value="#3d3d54"/>
      <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="Padding" Value="12,10"/>
  <Setter Property="FontSize" Value="14"/>
 <Setter Property="CaretBrush" Value="White"/>
        </Style>

  <!-- Dark ComboBox Template -->
 <ControlTemplate x:Key="DarkComboBoxTemplate" TargetType="ComboBox">
     <Grid>
  <Border x:Name="Border" Background="#2d2d44" BorderBrush="#3d3d54" BorderThickness="1" CornerRadius="5"/>
    <TextBlock Margin="15,0,40,0"
       VerticalAlignment="Center"
        Foreground="White"
      FontSize="14"
     Text="{TemplateBinding Text}"/>
    <Path Data="M0,0 L5,5 L10,0" 
      Fill="#888888" 
    HorizontalAlignment="Right"
    VerticalAlignment="Center"
       Margin="0,0,15,0"/>
     <ToggleButton IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
     Background="Transparent"
      BorderThickness="0"
  Focusable="False"
    ClickMode="Press">
       <ToggleButton.Template>
           <ControlTemplate TargetType="ToggleButton">
 <Border Background="Transparent"/>
    </ControlTemplate>
</ToggleButton.Template>
       </ToggleButton>
<Popup IsOpen="{TemplateBinding IsDropDownOpen}" 
        Placement="Bottom"
 PopupAnimation="Slide"
      AllowsTransparency="True"
       Width="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
     <Border Background="#2d2d44" 
   BorderBrush="#3d3d54" 
   BorderThickness="1" 
      CornerRadius="5"
   Margin="0,2,0,0">
   <ScrollViewer MaxHeight="200" VerticalScrollBarVisibility="Auto">
<ItemsPresenter/>
       </ScrollViewer>
      </Border>
          </Popup>
     </Grid>
            <ControlTemplate.Triggers>
   <Trigger Property="IsMouseOver" Value="True">
       <Setter TargetName="Border" Property="BorderBrush" Value="#8b3dff"/>
       </Trigger>
      </ControlTemplate.Triggers>
      </ControlTemplate>

   <!-- ComboBoxItem Style -->
   <Style TargetType="ComboBoxItem">
  <Setter Property="Background" Value="Transparent"/>
    <Setter Property="Foreground" Value="White"/>
       <Setter Property="Padding" Value="12,10"/>
       <Setter Property="FontSize" Value="14"/>
       <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
       <Setter.Value>
   <ControlTemplate TargetType="ComboBoxItem">
 <Border x:Name="Border" 
    Background="{TemplateBinding Background}" 
       Padding="{TemplateBinding Padding}"
CornerRadius="3">
       <ContentPresenter/>
     </Border>
         <ControlTemplate.Triggers>
   <Trigger Property="IsMouseOver" Value="True">
    <Setter TargetName="Border" Property="Background" Value="#3d3d54"/>
   </Trigger>
  <Trigger Property="IsSelected" Value="True">
       <Setter TargetName="Border" Property="Background" Value="#8b3dff"/>
   </Trigger>
          </ControlTemplate.Triggers>
       </ControlTemplate>
           </Setter.Value>
     </Setter>
        </Style>

    <!-- ComboBox Style -->
        <Style x:Key="FormComboBox" TargetType="ComboBox">
      <Setter Property="Background" Value="#2d2d44"/>
  <Setter Property="Foreground" Value="White"/>
   <Setter Property="BorderBrush" Value="#3d3d54"/>
   <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="FontSize" Value="14"/>
        <Setter Property="Template" Value="{StaticResource DarkComboBoxTemplate}"/>
        </Style>

 <!-- RadioButton Style -->
     <Style x:Key="AnswerRadioButton" TargetType="RadioButton">
  <Setter Property="Foreground" Value="White"/>
    <Setter Property="FontSize" Value="14"/>
  <Setter Property="Margin" Value="0,5"/>
  <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <!-- Button Style -->
   <Style x:Key="ActionButton" TargetType="Button">
      <Setter Property="Background" Value="#8b3dff"/>
 <Setter Property="Foreground" Value="White"/>
    <Setter Property="BorderThickness" Value="0"/>
   <Setter Property="FontSize" Value="14"/>
 <Setter Property="FontWeight" Value="SemiBold"/>
       <Setter Property="Cursor" Value="Hand"/>
   <Setter Property="Padding" Value="20,12"/>
  <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
            <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
 <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
   </Border>
   <ControlTemplate.Triggers>
   <Trigger Property="IsMouseOver" Value="True">
 <Setter Property="Background" Value="#9b4dff"/>
             </Trigger>
           </ControlTemplate.Triggers>
           </ControlTemplate>
     </Setter.Value>
         </Setter>
        </Style>

   <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
    <Setter Property="Background" Value="#28a745"/>
     <Style.Triggers>
<Trigger Property="IsMouseOver" Value="True">
      <Setter Property="Background" Value="#34ce57"/>
       </Trigger>
     </Style.Triggers>
      </Style>

        <Style x:Key="CancelButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
<Setter Property="Background" Value="#4d4d6d"/>
  <Style.Triggers>
   <Trigger Property="IsMouseOver" Value="True">
       <Setter Property="Background" Value="#5d5d7d"/>
    </Trigger>
 </Style.Triggers>
        </Style>
 </Window.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
    <Grid Margin="30">
            <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    <RowDefinition Height="Auto"/>
     </Grid.RowDefinitions>

    <!-- Header -->
  <StackPanel Grid.Row="0" Margin="0,0,0,25">
       <TextBlock Text="Edit Question" FontSize="24" FontWeight="Bold" Foreground="White"/>
     <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
      <TextBlock Text="Question ID: " FontSize="14" Foreground="#888888"/>
    <TextBlock x:Name="QuestionIdText" Text="" FontSize="14" Foreground="#8b3dff" FontWeight="Bold"/>
 </StackPanel>
   </StackPanel>

 <!-- Form -->
<StackPanel Grid.Row="1">
        <!-- Subject Selection -->
  <TextBlock Text="Subject *" FontSize="13" Foreground="#b0b0c8" Margin="0,0,0,8"/>
   <ComboBox x:Name="SubjectComboBox" Style="{StaticResource FormComboBox}" 
          Height="45" Margin="0,0,0,20"
        DisplayMemberPath="SubjectName"/>

<!-- Question Text -->
<TextBlock Text="Question Text *" FontSize="13" Foreground="#b0b0c8" Margin="0,0,0,8"/>
    <TextBox x:Name="QuestionTextBox" Style="{StaticResource FormTextBox}" 
TextWrapping="Wrap" AcceptsReturn="True" Height="80"
        VerticalScrollBarVisibility="Auto" Margin="0,0,0,20"/>

<!-- Options -->
   <TextBlock Text="Answer Options *" FontSize="13" Foreground="#b0b0c8" Margin="0,0,0,8"/>
    <Border Background="#252540" CornerRadius="8" Padding="15" Margin="0,0,0,20">
   <Grid>
       <Grid.RowDefinitions>
    <RowDefinition Height="Auto"/>
  <RowDefinition Height="Auto"/>
 <RowDefinition Height="Auto"/>
       <RowDefinition Height="Auto"/>
 </Grid.RowDefinitions>
 <Grid.ColumnDefinitions>
       <ColumnDefinition Width="40"/>
    <ColumnDefinition Width="*"/>
 </Grid.ColumnDefinitions>

       <!-- Option A -->
     <TextBlock Grid.Row="0" Grid.Column="0" Text="A)" FontSize="14" FontWeight="Bold" 
  Foreground="#8b3dff" VerticalAlignment="Center" Margin="0,0,0,10"/>
   <TextBox x:Name="OptionATextBox" Grid.Row="0" Grid.Column="1" 
    Style="{StaticResource FormTextBox}" MaxLength="255" Margin="0,0,0,10"/>

       <!-- Option B -->
   <TextBlock Grid.Row="1" Grid.Column="0" Text="B)" FontSize="14" FontWeight="Bold" 
Foreground="#8b3dff" VerticalAlignment="Center" Margin="0,0,0,10"/>
       <TextBox x:Name="OptionBTextBox" Grid.Row="1" Grid.Column="1" 
  Style="{StaticResource FormTextBox}" MaxLength="255" Margin="0,0,0,10"/>

    <!-- Option C -->
     <TextBlock Grid.Row="2" Grid.Column="0" Text="C)" FontSize="14" FontWeight="Bold" 
      Foreground="#8b3dff" VerticalAlignment="Center" Margin="0,0,0,10"/>
  <TextBox x:Name="OptionCTextBox" Grid.Row="2" Grid.Column="1" 
      Style="{StaticResource FormTextBox}" MaxLength="255" Margin="0,0,0,10"/>

    <!-- Option D -->
      <TextBlock Grid.Row="3" Grid.Column="0" Text="D)" FontSize="14" FontWeight="Bold" 
     Foreground="#8b3dff" VerticalAlignment="Center"/>
      <TextBox x:Name="OptionDTextBox" Grid.Row="3" Grid.Column="1" 
     Style="{StaticResource FormTextBox}" MaxLength="255"/>
     </Grid>
         </Border>

   <!-- Correct Answer Selection -->
      <TextBlock Text="Correct Answer *" FontSize="13" Foreground="#b0b0c8" Margin="0,0,0,8"/>
      <Border Background="#252540" CornerRadius="8" Padding="15" Margin="0,0,0,15">
         <StackPanel Orientation="Horizontal">
  <RadioButton x:Name="AnswerA" Content="A" GroupName="CorrectAnswer" 
 Style="{StaticResource AnswerRadioButton}" Margin="0,0,30,0"/>
      <RadioButton x:Name="AnswerB" Content="B" GroupName="CorrectAnswer" 
Style="{StaticResource AnswerRadioButton}" Margin="0,0,30,0"/>
        <RadioButton x:Name="AnswerC" Content="C" GroupName="CorrectAnswer" 
   Style="{StaticResource AnswerRadioButton}" Margin="0,0,30,0"/>
      <RadioButton x:Name="AnswerD" Content="D" GroupName="CorrectAnswer" 
  Style="{StaticResource AnswerRadioButton}"/>
     </StackPanel>
     </Border>
     </StackPanel>

            <!-- Buttons -->
    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,25,0,0">
       <Button Content="Cancel" Style="{StaticResource CancelButton}" 
       Click="CancelButton_Click" Width="120" Margin="0,0,15,0"/>
<Button Content="Save Changes" Style="{StaticResource SuccessButton}" 
        Click="SaveButton_Click" Width="150"/>
            </StackPanel>
  </Grid>
    </ScrollViewer>
</Window>
