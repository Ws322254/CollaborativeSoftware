<Window x:Class="CollaborativeSoftware.StudentDashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quiz Platform - Student Dashboard" Height="800" Width="1400"
   WindowStartupLocation="CenterScreen"
        Background="#1a1a2e"
Loaded="Window_Loaded">

    <Window.Resources>
      <!-- Toggle Button Style -->
        <Style x:Key="NavToggleButton" TargetType="Button">
            <Setter Property="Background" Value="#2d2d44"/>
 <Setter Property="Foreground" Value="#8b3dff"/>
       <Setter Property="BorderThickness" Value="0"/>
   <Setter Property="FontSize" Value="20"/>
            <Setter Property="Cursor" Value="Hand"/>
         <Setter Property="Template">
     <Setter.Value>
      <ControlTemplate TargetType="Button">
           <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="10">
    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
      </Border>
        <ControlTemplate.Triggers>
        <Trigger Property="IsMouseOver" Value="True">
      <Setter Property="Background" Value="#3d3d54"/>
  </Trigger>
     </ControlTemplate.Triggers>
      </ControlTemplate>
      </Setter.Value>
       </Setter>
        </Style>

        <!-- Navigation Button Style -->
  <Style x:Key="NavMenuButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
      <Setter Property="Foreground" Value="#b0b0c8"/>
      <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
 <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="Template">
                <Setter.Value>
      <ControlTemplate TargetType="Button">
          <Border x:Name="border" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" CornerRadius="5" Margin="5,2">
    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"/>
       </Border>
      <ControlTemplate.Triggers>
         <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="border" Property="Background" Value="#2d2d44"/>
          <Setter Property="Foreground" Value="#8b3dff"/>
             </Trigger>
     </ControlTemplate.Triggers>
            </ControlTemplate>
                </Setter.Value>
      </Setter>
        </Style>

        <!-- Action Button Style -->
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#8b3dff"/>
            <Setter Property="Foreground" Value="White"/>
    <Setter Property="BorderThickness" Value="0"/>
   <Setter Property="FontSize" Value="13"/>
      <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="15,10"/>
<Setter Property="Template">
   <Setter.Value>
        <ControlTemplate TargetType="Button">
     <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
         <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
    </Border>
        <ControlTemplate.Triggers>
   <Trigger Property="IsMouseOver" Value="True">
          <Setter Property="Background" Value="#9b4dff"/>
    </Trigger>
        </ControlTemplate.Triggers>
     </ControlTemplate>
      </Setter.Value>
      </Setter>
        </Style>

   <!-- Success Button Style -->
        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#28a745"/>
       <Style.Triggers>
      <Trigger Property="IsMouseOver" Value="True">
     <Setter Property="Background" Value="#34ce57"/>
                </Trigger>
   </Style.Triggers>
        </Style>

   <!-- Danger Button Style -->
     <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
<Setter Property="Background" Value="#dc3545"/>
    <Style.Triggers>
        <Trigger Property="IsMouseOver" Value="True">
        <Setter Property="Background" Value="#e4606d"/>
                </Trigger>
</Style.Triggers>
        </Style>

        <!-- Info Button Style -->
   <Style x:Key="InfoButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
      <Setter Property="Background" Value="#17a2b8"/>
       <Style.Triggers>
  <Trigger Property="IsMouseOver" Value="True">
     <Setter Property="Background" Value="#1fc8e3"/>
      </Trigger>
   </Style.Triggers>
        </Style>

        <!-- DataGrid Style -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#1a1a2e"/>
            <Setter Property="Foreground" Value="White"/>
          <Setter Property="BorderBrush" Value="#3d3d54"/>
       <Setter Property="BorderThickness" Value="1"/>
     <Setter Property="RowBackground" Value="#1a1a2e"/>
            <Setter Property="AlternatingRowBackground" Value="#252540"/>
          <Setter Property="GridLinesVisibility" Value="Horizontal"/>
    <Setter Property="HorizontalGridLinesBrush" Value="#3d3d54"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
   </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#2d2d44"/>
          <Setter Property="Foreground" Value="#8b3dff"/>
    <Setter Property="FontWeight" Value="Bold"/>
     <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderBrush" Value="#3d3d54"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
        </Style>

   <Style TargetType="DataGridCell">
      <Setter Property="Padding" Value="8,6"/>
  <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="Foreground" Value="White"/>
       <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
      <Setter Property="Background" Value="#8b3dff"/>
         <Setter Property="Foreground" Value="White"/>
   </Trigger>
  </Style.Triggers>
   </Style>

        <Style TargetType="DataGridRow">
        <Setter Property="Margin" Value="0,1"/>
          <Style.Triggers>
    <Trigger Property="IsSelected" Value="True">
    <Setter Property="Background" Value="#3d3d54"/>
        </Trigger>
     <Trigger Property="IsMouseOver" Value="True">
           <Setter Property="Background" Value="#2d2d44"/>
    </Trigger>
        </Style.Triggers>
        </Style>

        <!-- Stat Card Style -->
        <Style x:Key="StatCard" TargetType="Border">
    <Setter Property="Background" Value="#2d2d44"/>
       <Setter Property="CornerRadius" Value="10"/>
   <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,15,0"/>
        </Style>

    <!-- ListBox Style -->
        <Style x:Key="TestListBox" TargetType="ListBox">
   <Setter Property="Background" Value="#1a1a2e"/>
   <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3d3d54"/>
            <Setter Property="BorderThickness" Value="1"/>
   </Style>

        <Style x:Key="TestListBoxItem" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="15,12"/>
<Setter Property="Margin" Value="2"/>
   <Setter Property="Background" Value="#252540"/>
       <Setter Property="Foreground" Value="White"/>
         <Setter Property="Template">
      <Setter.Value>
       <ControlTemplate TargetType="ListBoxItem">
     <Border Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
      <ContentPresenter/>
       </Border>
 <ControlTemplate.Triggers>
        <Trigger Property="IsMouseOver" Value="True">
     <Setter Property="Background" Value="#3d3d54"/>
             </Trigger>
   <Trigger Property="IsSelected" Value="True">
    <Setter Property="Background" Value="#8b3dff"/>
           </Trigger>
      </ControlTemplate.Triggers>
  </ControlTemplate>
           </Setter.Value>
    </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
     <ColumnDefinition Width="260"/>
            <ColumnDefinition Width="*"/>
  </Grid.ColumnDefinitions>

    <!-- Side Navigation Bar -->
        <Border Grid.Column="0" Background="#0f0f1e" BorderBrush="#2d2d44" BorderThickness="0,0,1,0">
            <Grid>
           <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
                 <RowDefinition Height="Auto"/>
               <RowDefinition Height="*"/>
  <RowDefinition Height="Auto"/>
     </Grid.RowDefinitions>

       <!-- Header with Toggle -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="15,15,15,5">
       <Button x:Name="ToggleNavButton" Click="ToggleNav_Click" Style="{StaticResource NavToggleButton}" 
      Width="40" Height="40" Content="="/>
     <TextBlock Text="Quiz Platform" FontSize="18" FontWeight="Bold" Foreground="#8b3dff" 
       VerticalAlignment="Center" Margin="15,0,0,0"/>
           </StackPanel>

      <!-- User Info Section -->
      <Border Grid.Row="1" Padding="15" Background="#1a1a2e" Margin="10,10,10,5" CornerRadius="10">
       <StackPanel>
   <StackPanel Orientation="Horizontal">
      <Border Width="45" Height="45" CornerRadius="22" Background="#00d4ff" Margin="0,0,12,0">
      <TextBlock Text="S" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White"/>
     </Border>
   <StackPanel VerticalAlignment="Center">
 <TextBlock x:Name="UserNameBlock" Text="Student" FontSize="14" FontWeight="Bold" Foreground="White"/>
   <TextBlock Text="Student Account" FontSize="11" Foreground="#888888"/>
             </StackPanel>
 </StackPanel>
        </StackPanel>
        </Border>

    <!-- Navigation Menu -->
    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
          <StackPanel Margin="5,10,5,10">
      <TextBlock Text="MAIN MENU" FontSize="11" Foreground="#666" Margin="15,10,15,10" FontWeight="Bold"/>

  <Button x:Name="DashboardNavItem" Click="NavDashboard_Click" Style="{StaticResource NavMenuButton}">
  <StackPanel Orientation="Horizontal">
           <TextBlock Text="[D]" FontSize="14" FontWeight="Bold" Margin="0,0,12,0" Foreground="#8b3dff"/>
   <TextBlock Text="Dashboard" VerticalAlignment="Center" FontSize="14"/>
  </StackPanel>
   </Button>

        <Button x:Name="TestsNavItem" Click="NavTests_Click" Style="{StaticResource NavMenuButton}">
  <StackPanel Orientation="Horizontal">
   <TextBlock Text="[T]" FontSize="14" FontWeight="Bold" Margin="0,0,12,0" Foreground="#28a745"/>
    <TextBlock Text="Available Tests" VerticalAlignment="Center" FontSize="14"/>
  </StackPanel>
    </Button>

    <Button x:Name="ResultsNavItem" Click="NavResults_Click" Style="{StaticResource NavMenuButton}">
  <StackPanel Orientation="Horizontal">
      <TextBlock Text="[R]" FontSize="14" FontWeight="Bold" Margin="0,0,12,0" Foreground="#00d4ff"/>
      <TextBlock Text="My Results" VerticalAlignment="Center" FontSize="14"/>
         </StackPanel>
     </Button>

      <Button x:Name="LeaderboardNavItem" Click="NavLeaderboard_Click" Style="{StaticResource NavMenuButton}">
         <StackPanel Orientation="Horizontal">
           <TextBlock Text="[L]" FontSize="14" FontWeight="Bold" Margin="0,0,12,0" Foreground="#ffc107"/>
 <TextBlock Text="Leaderboard" VerticalAlignment="Center" FontSize="14"/>
  </StackPanel>
   </Button>
        </StackPanel>
         </ScrollViewer>

    <!-- Logout Button -->
       <Border Grid.Row="3" Margin="10,5,10,15">
    <Button Click="Logout_Click" Style="{StaticResource DangerButton}" Height="45">
    <StackPanel Orientation="Horizontal">
             <TextBlock Text="Exit" FontSize="14" FontWeight="Bold" Margin="0,0,10,0"/>
     <TextBlock Text="Logout" VerticalAlignment="Center" FontSize="14"/>
      </StackPanel>
           </Button>
       </Border>
       </Grid>
        </Border>

     <!-- Main Content Area -->
        <Grid Grid.Column="1" Margin="25,20,25,20">
        <Grid.RowDefinitions>
  <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
     <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

        <!-- Page Title -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock x:Name="PageTitleIcon" Text="" FontSize="24" FontWeight="Bold" Foreground="#8b3dff" Margin="0,0,15,0" VerticalAlignment="Center"/>
         <StackPanel>
     <TextBlock x:Name="PageTitle" Text="Dashboard" FontSize="28" FontWeight="Bold" Foreground="White"/>
  <TextBlock x:Name="PageSubtitle" Text="Welcome back! Here is your progress overview." FontSize="14" Foreground="#888888"/>
 </StackPanel>
        </StackPanel>

        <!-- Statistics Row -->
      <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,25">
         <Border Style="{StaticResource StatCard}" Width="200">
          <StackPanel>
            <TextBlock Text="Total Points" FontSize="13" Foreground="#888888" Margin="0,0,0,8"/>
            <TextBlock x:Name="TotalPointsBlock" Text="0" FontSize="36" FontWeight="Bold" Foreground="#8b3dff"/>
      <TextBlock Text="Points earned" FontSize="11" Foreground="#666"/>
         </StackPanel>
     </Border>

       <Border Style="{StaticResource StatCard}" Width="200">
                    <StackPanel>
          <TextBlock Text="Average Score" FontSize="13" Foreground="#888888" Margin="0,0,0,8"/>
       <TextBlock x:Name="AverageScoreBlock" Text="0%" FontSize="36" FontWeight="Bold" Foreground="#00d4ff"/>
        <TextBlock Text="Overall average" FontSize="11" Foreground="#666"/>
  </StackPanel>
    </Border>

        <Border Style="{StaticResource StatCard}" Width="200">
         <StackPanel>
         <TextBlock Text="Tests Completed" FontSize="13" Foreground="#888888" Margin="0,0,0,8"/>
     <TextBlock x:Name="TestsCompletedBlock" Text="0" FontSize="36" FontWeight="Bold" Foreground="#28a745"/>
      <TextBlock Text="Tests taken" FontSize="11" Foreground="#666"/>
     </StackPanel>
    </Border>

    <Border Style="{StaticResource StatCard}" Width="200">
   <StackPanel>
           <TextBlock Text="Available Tests" FontSize="13" Foreground="#888888" Margin="0,0,0,8"/>
               <TextBlock x:Name="AvailableTestsBlock" Text="0" FontSize="36" FontWeight="Bold" Foreground="#ffc107"/>
       <TextBlock Text="Ready to take" FontSize="11" Foreground="#666"/>
            </StackPanel>
          </Border>
            </StackPanel>

  <!-- Content Panels -->
   <Grid Grid.Row="2">
    <!-- Dashboard Panel -->
       <Border x:Name="DashboardPanel" Background="#2d2d44" CornerRadius="10" Padding="20" Visibility="Visible">
               <StackPanel>
  <TextBlock Text="Quick Start" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,15"/>
         <TextBlock Text="Welcome to your student dashboard!" FontSize="14" Foreground="#b0b0c8" Margin="0,0,0,15"/>
             
    <Border Background="#1a1a2e" CornerRadius="8" Padding="15" Margin="0,0,0,10">
        <StackPanel>
        <TextBlock Text="Getting Started" FontSize="15" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,10"/>
       <TextBlock Text="- Go to Available Tests to see tests you can take" FontSize="13" Foreground="#888888" Margin="0,5"/>
           <TextBlock Text="- Select a test and click Start Test to begin" FontSize="13" Foreground="#888888" Margin="0,5"/>
     <TextBlock Text="- View your results in My Results" FontSize="13" Foreground="#888888" Margin="0,5"/>
      <TextBlock Text="- Check your ranking on the Leaderboard" FontSize="13" Foreground="#888888" Margin="0,5"/>
         </StackPanel>
             </Border>

             <Border Background="#1a1a2e" CornerRadius="8" Padding="15">
        <StackPanel>
      <TextBlock Text="Your Progress" FontSize="15" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,10"/>
        <TextBlock x:Name="ProgressSummary" Text="Loading your progress..." FontSize="13" Foreground="#b0b0c8"/>
     </StackPanel>
                </Border>
          </StackPanel>
    </Border>

     <!-- Tests Panel -->
       <Border x:Name="TestsPanel" Background="#2d2d44" CornerRadius="10" Padding="20" Visibility="Collapsed">
           <Grid>
                  <Grid.RowDefinitions>
           <RowDefinition Height="Auto"/>
   <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
     </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
  <TextBlock Text="Available Tests" FontSize="18" FontWeight="Bold" Foreground="White"/>
              <TextBlock Text=" - Select a test to start" FontSize="14" Foreground="#888888" VerticalAlignment="Center" Margin="10,0,0,0"/>
      </StackPanel>

          <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
  <Button Content="Refresh Tests" Click="RefreshTests_Click" Style="{StaticResource InfoButton}"/>
          </StackPanel>

  <ListBox x:Name="QuizzesListBox" Grid.Row="2" 
        Style="{StaticResource TestListBox}"
      ItemContainerStyle="{StaticResource TestListBoxItem}"
    MouseDoubleClick="QuizzesListBox_MouseDoubleClick"/>

       <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,15,0,0">
     <Button Content="Start Selected Test" Click="StartTestButton_Click" Style="{StaticResource SuccessButton}" Margin="0,0,10,0"/>
    <Button Content="View Test Details" Click="ViewTestDetails_Click" Style="{StaticResource InfoButton}"/>
          </StackPanel>
       </Grid>
                </Border>

        <!-- Results Panel -->
             <Border x:Name="ResultsPanel" Background="#2d2d44" CornerRadius="10" Padding="20" Visibility="Collapsed">
              <Grid>
    <Grid.RowDefinitions>
     <RowDefinition Height="Auto"/>
          <RowDefinition Height="Auto"/>
   <RowDefinition Height="*"/>
  <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
 <TextBlock Text="My Results" FontSize="18" FontWeight="Bold" Foreground="White"/>
   <TextBlock Text=" - View your test history and scores" FontSize="14" Foreground="#888888" VerticalAlignment="Center" Margin="10,0,0,0"/>
          </StackPanel>

     <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
  <Button Content="Refresh Results" Click="RefreshResults_Click" Style="{StaticResource InfoButton}"/>
       </StackPanel>

     <DataGrid x:Name="ResultsDataGrid" Grid.Row="2" 
       AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
    SelectionMode="Single" SelectionUnit="FullRow">
        <DataGrid.Columns>
           <DataGridTextColumn Header="Test Name" Binding="{Binding TestName}" Width="200"/>
      <DataGridTextColumn Header="Subject" Binding="{Binding SubjectName}" Width="150"/>
     <DataGridTextColumn Header="Score" Binding="{Binding ScorePercentage, StringFormat={}{0:F1}%}" Width="100"/>
        <DataGridTextColumn Header="Points" Binding="{Binding TotalPoints}" Width="80"/>
    <DataGridTextColumn Header="Date" Binding="{Binding AttemptDate, StringFormat='dd MMM yyyy HH:mm'}" Width="140"/>
     </DataGrid.Columns>
           </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,15,0,0">
    <Button Content="View Details" Click="ViewResultDetails_Click" Style="{StaticResource ActionButton}" Margin="0,0,10,0"/>
           <Button Content="Full Results Window" Click="OpenResultsWindow_Click" Style="{StaticResource InfoButton}"/>
     </StackPanel>
 </Grid>
  </Border>

  <!-- Leaderboard Panel -->
       <Border x:Name="LeaderboardPanel" Background="#2d2d44" CornerRadius="10" Padding="20" Visibility="Collapsed">
   <Grid>
              <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    <RowDefinition Height="Auto"/>
</Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
<TextBlock Text="Leaderboard" FontSize="18" FontWeight="Bold" Foreground="White"/>
         <TextBlock Text=" - Top performing students" FontSize="14" Foreground="#888888" VerticalAlignment="Center" Margin="10,0,0,0"/>
    </StackPanel>

         <Border Grid.Row="1" Background="#1a1a2e" CornerRadius="8" Padding="15" Margin="0,0,0,15">
   <StackPanel Orientation="Horizontal">
       <TextBlock Text="Your Rank: " FontSize="14" Foreground="#888888" VerticalAlignment="Center"/>
         <TextBlock x:Name="YourRankBlock" Text="#--" FontSize="18" FontWeight="Bold" Foreground="#ffc107" VerticalAlignment="Center"/>
  <TextBlock Text="  |  " FontSize="14" Foreground="#444" VerticalAlignment="Center"/>
 <TextBlock Text="Total Points: " FontSize="14" Foreground="#888888" VerticalAlignment="Center"/>
    <TextBlock x:Name="YourTotalPointsBlock" Text="0" FontSize="18" FontWeight="Bold" Foreground="#8b3dff" VerticalAlignment="Center"/>
      </StackPanel>
       </Border>

     <DataGrid x:Name="LeaderboardDataGrid" Grid.Row="2" 
     AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
      SelectionMode="Single" SelectionUnit="FullRow">
       <DataGrid.Columns>
         <DataGridTextColumn Header="Rank" Binding="{Binding Rank}" Width="70"/>
           <DataGridTextColumn Header="Student" Binding="{Binding StudentName}" Width="200"/>
  <DataGridTextColumn Header="Tests Taken" Binding="{Binding TestsTaken}" Width="100"/>
   <DataGridTextColumn Header="Avg Score" Binding="{Binding AverageScore, StringFormat={}{0:F1}%}" Width="100"/>
           <DataGridTextColumn Header="Total Points" Binding="{Binding TotalPoints}" Width="120"/>
     </DataGrid.Columns>
         </DataGrid>

         <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,15,0,0">
               <Button Content="Refresh Leaderboard" Click="RefreshLeaderboard_Click" Style="{StaticResource InfoButton}" Margin="0,0,10,0"/>
      <Button Content="Full Leaderboard Window" Click="OpenLeaderboardWindow_Click" Style="{StaticResource ActionButton}"/>
 </StackPanel>
        </Grid>
                </Border>
       </Grid>
        </Grid>
    </Grid>
</Window>
